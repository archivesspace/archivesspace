<%= link_to I18n.t('skipnav.main_content'), '#container-fluid', :class => 'sr-only' %>

<% if defined?(@search) && !defined?(@results) %>
  <%= link_to I18n.t('skipnav.search'), '#advanced_search', :class => 'sr-only' %>
<% elsif defined?(@search) && !defined?(@no_statement) %>
  <%= link_to I18n.t('skipnav.search'), '#toggleRefineSearch', :class => 'sr-only' %>
<% end %>
