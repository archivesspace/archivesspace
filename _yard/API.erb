# ArchivesSpace REST API
As of <%= @time.inspect %> the following REST endpoints exist:

<% @endpoints.each do | e | %>
	
## <%= e[:method].upcase%> <%= e[:uri]%> 

### Description

<%= e[:description]%>


### Parameters
<% e[:params].each do |param| 
  opts = (param[3] or {})
  vs = opts[:validation] ? " -- #{opts[:validation][0]}" : "" %>
<% if opts[:body] %>
	<%= "#{param[1]} <request body> -- #{param[2]}#{vs}" %>
<% else %>
	<%= "#{param[1]} #{param[0]} -- #{param[2]}#{vs}" %>
<% end %>
<% end %>

### Returns

<% e[:returns].each do |ret| %>
<%= "\t#{ret[0]} -- #{ret[1]}\n" %>
<% end %>

<% end %>


