<% (record['context'] || []).each_with_index do |title, i| %>
  <%= resolve_readonly_link_to((clean_mixed_content(title).html_safe), record['context_uri'][i]) %>
  <% if i < record['context'].length - 1 %>
    <%= context_separator(record) %>
  <% end %>
<% end %>
