<%
   if form.obj['ref'].blank?
     selected_json = "{}"
   elsif  form.obj['resolved']['ref'].is_a?(String)
     selected_json =  JSON(form.obj['resolved']['ref']).to_json
   else
     selected_json =  form.obj['resolved']['ref'].to_json
   end
%>
<div class="control-group">
  <label class="control-label">Agent</label>
  <div class="controls">
    <div class="input-append linker-wrapper">
      <input type="text" class="linker"
             id="<%= form.id_for("ref") %>"
             data-label="Agent"
             data-label_plural="Agents"
             data-name="ref"
             data-path="<%= form.path %>"
             data-url="<%= url_for :controller => :search, :action => :do_search, :format => :json %>"
             data-selected='<%= selected_json %>'
             data-multiplicity="one"
             data-format_template="names[0].sort_name"
             data-types='["agent"]'
      />
      <div class="btn-group pull-right">
        <a class="btn dropdown-toggle last" data-toggle="dropdown" href="javascript:void(0);"><span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><a href="javascript:void(0);" class="linker-browse-btn">Browse</a></li>
          <li class="dropdown-submenu pull-left">
            <a href="javascript:void(0);">Create</a>
            <ul class="dropdown-menu">
              <li><a href="javascript:void(0);" data-target="<%= url_for :controller => :agents, :action => :new, :type => :agent_person, :inline => true %>" class="linker-create-btn">Person</a></li>
              <li><a href="javascript:void(0);" data-target="<%= url_for :controller => :agents, :action => :new, :type => :agent_family, :inline => true %>" class="linker-create-btn">Family</a></li>
              <li><a href="javascript:void(0);" data-target="<%= url_for :controller => :agents, :action => :new, :type => :agent_corporate_entity, :inline => true %>" class="linker-create-btn">Corporate Entity</a></li>
              <li><a href="javascript:void(0);" data-target="<%= url_for :controller => :agents, :action => :new, :type => :agent_software, :inline => true %>" class="linker-create-btn">Software</a></li>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </div>
</div>
