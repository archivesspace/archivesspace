<div id="archivesSpaceSidebar">
  <ul class="nav nav-list affix as-nav-list" data-spy="affix">
    <li><a href="#basic_information">Basic Information <span class="icon-chevron-right"></span></a></li>

    <% if controller.action_name != "show" || @accession.extents.length > 0 %>
      <li><a href="#accession_extents_">Extents <span class="icon-chevron-right"></span></a></li>
    <% end %>

    <% if not controller.action_name === "show" or @accession.dates.length > 0 %>
      <li><a href="#accession_dates_">Dates <span class="icon-chevron-right"></span></a></li>
    <% end %>

    <% if not controller.action_name === "show" or @accession.external_documents.length > 0 %>
      <li><a href="#accession_external_documents_">External Documents <span class="icon-chevron-right"></span></a></li>
    <% end %>

    <% if not controller.action_name === "show" or @accession.rights_statements.length > 0 %>
      <li><a href="#accession_rights_statements_">Rights Statements <span class="icon-chevron-right"></span></a></li>
    <% end %>

    <% if not controller.action_name === "show" %>
      <li class="form-actions">
        <button type="submit" class="btn btn-primary">Save Accession</button>
      </li>
    <% end %>
  </ul>
</div>