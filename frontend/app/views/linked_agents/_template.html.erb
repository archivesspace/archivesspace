<% define_template "accession_linked_agent", jsonmodel_definition(:accession, "linked_agents") do |form| %>
  <div class="subrecord-form-fields">
    <%= form.label_and_select("role", form.possible_options_for("role", true)) %>
    <%= form.label_and_select("relator", form.possible_options_for("relator", true)) %>
    <%= render :partial => "agents/linker", :locals => {:form => form} %>
  </div>
<% end %>

<% define_template "event_linked_agent", jsonmodel_definition(:event, "linked_agents") do |form| %>
  <div class="subrecord-form-fields">
    <%= form.label_and_select("role", form.possible_options_for("role", true)) %>
    <%= render :partial => "agents/linker", :locals => {:form => form} %>
  </div>
<% end %>

<% define_template "resource_linked_agent", jsonmodel_definition(:resource, "linked_agents") do |form| %>
  <div class="subrecord-form-fields">
    <%= form.label_and_select("role", form.possible_options_for("role", true)) %>
    <%= form.label_and_select("relator", form.possible_options_for("relator", true)) %>
    <%= render :partial => "agents/linker", :locals => {:form => form} %>
  </div>
<% end %>

<% define_template "archival_object_linked_agent", jsonmodel_definition(:archival_object, "linked_agents") do |form| %>
  <div class="subrecord-form-fields">
    <%= form.label_and_select("role", form.possible_options_for("role", true)) %>
    <%= form.label_and_select("relator", form.possible_options_for("relator", true)) %>
    <%= render :partial => "agents/linker", :locals => {:form => form} %>
  </div>
<% end %>

<% define_template "digital_object_linked_agent", jsonmodel_definition(:digital_object, "linked_agents") do |form| %>
  <div class="subrecord-form-fields">
    <%= form.label_and_select("role", form.possible_options_for("role", true)) %>
    <%= render :partial => "agents/linker", :locals => {:form => form} %>
  </div>
<% end %>

<% define_template "digital_object_component_linked_agent", jsonmodel_definition(:digital_object_component, "linked_agents") do |form| %>
  <div class="subrecord-form-fields">
    <%= form.label_and_select("role", form.possible_options_for("role", true)) %>
    <%= render :partial => "agents/linker", :locals => {:form => form} %>
  </div>
<% end %>
