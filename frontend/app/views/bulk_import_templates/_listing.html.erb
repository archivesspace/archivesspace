<%= render_aspace_partial :partial => "shared/flash_messages" %>

<% if @template_files.length %>
    <table class="table table-striped table-bordered table-condensed">
        <thead>
            <tr>
                <th><%= I18n.t("bulk_import_template._singular") %></th>
                <th width="70px"><span class="sr-only"><%= I18n.t('actions.download') %></span></th>
            </tr>
        </thead>
        <tbody>
            <% @template_files.each do |template| %>
                <tr>
                    <td><%= template[:name] %></td>
                    <td>
                        <div class="btn-group" style="float: right">
                            <%= link_to I18n.t("actions.download"), {:controller => :bulk_import_templates, :action => :download, :filename => template[:filename]}, :class => "btn btn-xs btn-default" %>
                        </div>
                    </td>
                </tr>
            <% end %>
        </tbody>
    </table>
<% else %>
    <p class="alert alert-info">
        No Bulk Import Templates.
    </p>
<% end %>
