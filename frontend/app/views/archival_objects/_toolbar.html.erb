<% if user_can?('update_resource_record') %>
  <%= render_aspace_partial(:partial => '/shared/component_toolbar',
             :locals => {
               :parent_link => {:controller => :resources, :action => :edit, :id => JSONModel(:resource).id_for(@archival_object.resource["ref"]), :anchor => "tree::archival_object_#{@archival_object.id}"},
               :record => @archival_object,
               :show_delete => user_can?('delete_archival_record')
              })
  %>
<% end %>
