<%= setup_context :object => @group, :title => (!@group.description.empty? ? @group.description : "#{I18n.t("actions.edit")} #{I18n.t("group._singular")}") %>

<%= form_for @group, :as => "group", :url => {:action => :update, :id => @group.id}, :html => {:class => 'form-horizontal aspace-record-form'}.merge(update_monitor_params(@group)) do |f| %>

<div class="row-fluid">
  <div class="span3">
  </div>


  <div class="span9">
    <%= render :partial => "toolbar", :formats => [:html], :handlers => [:erb] %>

    <div class="record-pane">
      <h2><%= !@group.description.empty? ? @group.description : "#{I18n.t("actions.edit")} #{I18n.t("group._singular")}" %> <span class="label label-info"><%= I18n.t("group._singular") %></span></h2>

      <%= form_context :group, @group do |form| %>
        <%= render :partial => "shared/form_messages", :formats => [:html], :handlers => [:erb], :locals => {:form => form} %>
        <%= render :partial => "groups/form", :formats => [:html], :handlers => [:erb], :locals => {:form => form} %>
      <% end %>
    </div>
  </div>
</div>
<% end %>

