<div id="archivesSpaceSidebar">
  <ul class="nav nav-list as-nav-list">
    <li><a href="#basic_information">Basic Information <span class="icon-chevron-right"></span></a></li>

    <% if controller.action_name != "show" || @digital_object_component.linked_agents.length > 0 %>
      <li><a href="#digital_object_component_linked_agents_">Agent Links <span class="icon-chevron-right"></span></a></li>
    <% end %>
    
    <% if not controller.action_name === "show" or @digital_object_component.notes.length > 0 %>
      <li><a href="#notes">Notes <span class="icon-chevron-right"></span></a></li>
    <% end %>

    <% if controller.action_name != "show" || @digital_object_component.extents.length > 0 %>
      <li><a href="#digital_object_component_extents_">Extents <span class="icon-chevron-right"></span></a></li>
    <% end %>

    <% if controller.action_name != "show" || @digital_object_component.dates.length > 0 %>
      <li><a href="#digital_object_component_dates_">Dates <span class="icon-chevron-right"></span></a></li>
    <% end %>

    <% if not controller.action_name === "show" or @digital_object_component.external_documents.length > 0 %>
      <li><a href="#digital_object_component_external_documents_">External Documents <span class="icon-chevron-right"></span></a></li>
    <% end %>

    <% if not controller.action_name === "show" %>
      <div class="form-actions">
        <% if controller.action_name === "new" or controller.action_name === "create" %>
          <div class="btn-group btn-submit-group">
            <button type="submit" class="btn btn-primary">Save Digital Object Component</button>
            <button type="button" id="createPlusOne" class="btn btn-primary">+1</button>
          </div>
        <% else %>
          <button type="submit" class="btn btn-primary">Save Digital Object Component</button>
        <% end %>
      </div>
    <% end %>
  </ul>
</div>