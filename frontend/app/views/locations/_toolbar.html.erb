<% if user_can?('update_location_record') %>
  <div class="row-fluid">
    <div class="span12">
      <div class="record-toolbar">
        <div class="pull-left">
          <div class="btn-group">
            <%= link_to I18n.t("actions.edit"), {:controller => :locations, :action => :edit, :id => @location.id}, :class => "btn btn-small btn-primary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
