<%= setup_context :object => @location, :title => @location.title %>

<div class="row-fluid">
   <div class="span3">
     <%= render :partial => "locations/sidebar", :formats => [:html], :handlers => [:erb] %>
   </div>
  <div class="span9">
    <%= render :partial => "toolbar", :formats => [:html], :handlers => [:erb] %>
    <div class="record-pane">
      <h2><%= @location.title %>  <span class="label label-info"><%= I18n.t("location._singular") %></span></h2>

      <%= render :partial => "shared/flash_messages", :formats => [:html], :handlers => [:erb] %>

      <%= read_only_view(@location.to_hash) %>

      <%= render :partial => "search/embedded", :formats => [:html], :handlers => [:erb], :locals => {:record => @location, :filter_term => {"location_uris" => @location.uri}.to_json, :heading_text => I18n.t("location._frontend.section.search_embedded")} %>
    </div>
  </div>
</div>
