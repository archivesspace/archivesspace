source 'http://rubygems.org'

gem 'rails', '3.2.18'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'activerecord-jdbcsqlite3-adapter'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'

  gem 'uglifier', '>= 1.0.3'
  gem 'therubyrhino'

  gem 'less-rails', '~> 2.3.3'
end

gem 'jquery-rails'
gem 'jquery-ui-rails'
gem "json", "1.8.0"
gem 'json-schema', '~> 1.0.10'

gem 'useragent', '~> 0.6.0'

gem "jruby-jars", "= 1.7.11"

gem 'warbler'

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'debugger'

gem 'atomic', '= 1.0.1'

# For queuing batch import jobs

#gem 'delayed_job_active_record'


group :test do
  gem 'rspec', '~> 2.12.0'
  gem 'rspec-core', '2.12.1'
  gem 'rspec-rails', '~> 2.12.1'
  gem 'simplecov', "0.7.1"
end

gem "puma"

gem "net-http-persistent"
gem "multipart-post"

gem "rubyzip", "1.0.0"
gem "zip-zip"

# The frontend doesn't specifically depend on Nokogiri at the moment, but
# several plugins use it.
gem "nokogiri", '~> 1.6.1'


# Allow plugins to provide their own Gemfiles too.
#
require 'asutils'
ASUtils.load_plugin_gems(self)
