<div class="row">
<div class="col-sm-9">
<h2><%= @hits %> <%= t('repository._plural')%></h2>

<% @json.each do |result| %>
<%= render partial: 'repositories/repository', locals: {:result => result, :full => false} %>

<% end %>
<%= render partial: 'shared/pagination', locals: {:pager => @pager} %>
</div>
</div>
