<table>
  <tr>
    <th>Location</th>
    <th>Container</th>
  </tr>

  <% @report.each do |location| %>
    <tr>
      <td><%= h location.fetch('location') %></td>
      <td><%= h location.fetch('container') %></td>
    </tr>
  <% end %>
</table>
