<table>
  <tr>
    <th>URI</th>
    <th>Use Statement</th>
    <% @report.each do |file_version| %>
    <tr>
      <td><%= h file_version.fetch('uri') %></td>
      <td><%= h file_version.fetch('useStatement') %></td>
    </tr>
  <% end %>
</table>
