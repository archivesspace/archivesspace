# The makings of some Selenium tests

## To run, the usual:

  * Run the bootstrap script to configure JRuby and all required
    dependencies:

         $ build/run bootstrap

  * Run the tests

         $ build/run selenium:test
