ArchivesSpace Pugs plugin
===============

This is an example plugin for the new ArchivesSpace public interface.  It add pugs.

## Getting Started

Enable the plugin by editing the file in `config/config.rb`:

    AppConfig[:plugins] = ['some_plugin', 'pugs']